Buffer {
    int8 *beginning;
    size_t start;
    size_t end;
}

len = end - start;
cap = 10 >> 20;
[-,-,z,k,a,1,2,y,y,y] = 10

start = 1
end = 5

[-,y,z,k,k];
[-,y,z,k,k,-,-,-];
[y,z,k,k,-,-,-,-];

if len + 1> cap || end >= cap :
    // resize and start from the beginning
    memcpy(new, arr[start], len);
    end = len;

[z,k,a,1,2,y,y,y, ...] = 20







